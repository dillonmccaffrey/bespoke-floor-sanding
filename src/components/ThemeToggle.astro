---
---

<button
  id="theme-toggle"
  class="relative w-14 h-7 rounded-full bg-background-lighter border border-text-muted/30 transition-colors focus:outline-none focus:ring-2 focus:ring-primary/50"
  aria-label="Toggle theme"
  role="switch"
  aria-checked="false"
>
  <!-- Track icons -->
  <span class="absolute inset-0 flex items-center justify-between px-1.5 pointer-events-none">
    <!-- Sun icon (left side) -->
    <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-primary" viewBox="0 0 20 20" fill="currentColor">
      <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd" />
    </svg>
    <!-- Moon icon (right side) -->
    <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-text-muted" viewBox="0 0 20 20" fill="currentColor">
      <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z" />
    </svg>
  </span>
  <!-- Toggle knob -->
  <span
    id="toggle-knob"
    class="absolute top-0.5 left-0.5 w-6 h-6 rounded-full bg-text shadow-md transition-transform duration-200"
  ></span>
</button>

<script>
  const toggle = document.getElementById('theme-toggle');
  const knob = document.getElementById('toggle-knob');
  const html = document.documentElement;

  function getSystemTheme() {
    return window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
  }

  function getStoredTheme() {
    return localStorage.getItem('theme');
  }

  function setTheme(theme: string) {
    if (theme === 'light') {
      html.classList.add('light');
      html.classList.remove('dark');
      knob?.classList.remove('translate-x-7');
      toggle?.setAttribute('aria-checked', 'true');
    } else {
      html.classList.add('dark');
      html.classList.remove('light');
      knob?.classList.add('translate-x-7');
      toggle?.setAttribute('aria-checked', 'false');
    }
    localStorage.setItem('theme', theme);
  }

  // Initialize theme
  const storedTheme = getStoredTheme();
  const initialTheme = storedTheme || getSystemTheme();
  setTheme(initialTheme);

  // Toggle on click
  toggle?.addEventListener('click', () => {
    const currentTheme = html.classList.contains('light') ? 'light' : 'dark';
    setTheme(currentTheme === 'light' ? 'dark' : 'light');
  });

  // Listen for system theme changes
  window.matchMedia('(prefers-color-scheme: light)').addEventListener('change', (e) => {
    if (!getStoredTheme()) {
      setTheme(e.matches ? 'light' : 'dark');
    }
  });
</script>
